---
name: translate-teaching-materials
description: Translate the Japanese student textbooks (docs/**/*.html) into the students' native languages through per-sentence translation catalogs under i18n/. Use when asked to translate teaching materials, prepare translations before a release, fix a translation, or review a translation PR in this repository.
---

# Translate teaching materials

日本語で書いた学生向けの教科書（`docs/**/*.html`）を、学生の母国語に展開する。翻訳済みのHTMLはコミットしない。コミットするのは、文単位の対訳カタログ `i18n/<言語>/<ページ>.json` と用語集 `i18n/<言語>/glossary.md` だけで、各言語のHTMLはカタログから作る。

## 前提

- **授業は日本語で進む。** 翻訳は、読んで理解するための補助である。先生が口にする言葉、画面に出る言葉と、訳文の言葉を、学生が結び付けられるようにする。
- **日本語版が正。** 日本語の教科書（`docs/`）は、翻訳の都合で書き換えない。原文が曖昧で訳せないときは、推測で訳さず、PR本文に挙げる。
- **訳すのは、未翻訳の文だけ。** 日本語の文を直すと、その文は自動で未翻訳に戻る（古い訳は外れ、学生には日本語で表示される）。変わっていない文の訳は触らない。
- 言語の一覧は `config/i18n.json`、言語ごとの用語集は `i18n/<言語>/glossary.md` にある。
- この授業では、学生の使うIDEが**IntelliJ IDEA（純Kotlin単元）と Android Studio（Android単元）の2つ**ある。**表示の言語は、教科書によって違う。** Android系の教科書は、英語表示の Android Studio で書いてある。K01の教科書は、日本語表示の IntelliJ IDEA で書いてある（教科書の足もとにそう書いてある）。IntelliJ IDEA は日本語・韓国語・中国語の言語パックを同梱していて、Macの言語に合わせて表示が変わることがある。

| 学生の母国語 | コード | 書き方 |
| --- | --- | --- |
| 英語 | `en` | |
| 中国語 | `zh-Hans` | 簡体字 |
| 広東語 | `zh-Hant-HK` | 繁体字の書き言葉に、香港の語彙を使う（程式、檔案、螢幕）。話し言葉をそのまま書く粤語白話文にはしない |
| 台湾華語 | `zh-Hant-TW` | 下の「台湾華語（`zh-Hant-TW`）の注意」を読む |
| 韓国語 | `ko` | 下の「韓国語（`ko`）の注意」を読む |
| ミャンマー語 | `my` | Unicodeで書く（Zawgyiではない） |
| スペイン語 | `es` | 下の「スペイン語（`es`）の注意」を読む |
| アラビア語 | `ar` | 右から左に書く。下の「アラビア語（`ar`）の注意」を読む |
| モンゴル語 | `mn` | 下の「モンゴル語（`mn`）の注意」を読む |

## 手順

配布していない言語（`distribute: false`）の翻訳PR（初回の翻訳、ページごとの翻訳）は、この手順で訳し、`check` と `build` で作った確認用ページの目視までで終える（照合は下の「別のモデルによる照合」の表を見る）。**この翻訳PRが開いているあいだも、`docs/` を触るPRのマージは止めない**（学生向けの公開がないので、止める理由がない。#104）。`docs/` の日本語が先に変わったら、翻訳PRの側が最新のmainを取り込む。変わった文は、そのPRで訳しても、日本語のまま残してもよい（#100）。`docs/` のマージを止めるのは、配布準備の翻訳PR（下の「配布前の差分翻訳」）のあいだだけである。

1. 未翻訳の文を書き出す。

   ```sh
   python3 scripts/localize-student-materials.py sync --lang en
   ```

   カタログが今の日本語に合わせて整理され（使わなくなった訳が外れる）、`dist/i18n-work/<言語>/<ページ>/todo-NNN.json` ができる（`dist/` はGit管理の対象外）。1ファイルは40文ほど。同じ文が複数のページにあるときは、最初のページの作業ファイルにだけ出る。1回訳せば、全ページに入る。ページを絞るときは `--page docs/hello-kotlin/index.html` を付ける。
2. 訳す前に、そのページの日本語のHTML（`docs/…`）を通して読む。文だけを見ても、見出しなのか、ボタンの名前なのか、表のどの列なのかが分からない。作業ファイルの `where` は、その文がどの要素にあるかを示す（`h2`、`td`、`img alt` など）。
3. 作業ファイル1つにつき、`done_file` に書いてあるパスへ、訳を `id` と訳文の組だけのJSONで書く。原文は書かない。

   ```json
   {
     "0dce59d92673": "Three goals",
     "4e8d3eaf93d9": "Click <strong>Run</strong> and check the console output."
   }
   ```

   `previous_source` と `previous_translation` が付いている文は、日本語が少しだけ直された文である。ゼロから訳し直さず、前の訳を、変わった分だけ直す。
4. 検査する。落ちた訳は、理由が出るので直す。

   ```sh
   python3 scripts/localize-student-materials.py merge --lang en --dry-run dist/i18n-work/en/hello-kotlin/index/done-001.json
   ```

   「検査に通った訳」が、そのファイルで訳した文の数と同じなら、全部通っている。「カタログに入る数」のほうが多いのは、同じ原文がほかのページにもあり、そこにも入るためである。
5. カタログへ入れる。`--dry-run` を外す。ファイルを省くと、その言語の `done-*.json` をすべて入れる。すでに訳がある文は入れ替えない（入れ替えるときは `--overwrite`。1文だけ直すなら、カタログの `translation` を直接直してよい）。
6. 確かめる。

   ```sh
   python3 scripts/localize-student-materials.py check
   python3 scripts/localize-student-materials.py status --lang en
   python3 scripts/localize-student-materials.py build --lang en
   ```

   `build` は `dist/i18n-preview/docs/<言語>/…` にページを作る。`file://` で開くとCSSが当たらないことがあるので、ローカルのサーバーで開く。レイアウトの崩れ、リンク、コードのコピー、共通資料の「もとの教科書へ戻る」を確かめる。コピーボタンと進捗表示は、配布物と同じ `config/i18n.json` の `languages[].ui` で出るので、本文の説明とボタンの文言が食い違っていないかもここで見る。

   確認用ページには、言語の切り替え（`language-nav`）と翻訳の注記（`translation-note`）は入らない。この2つは配布対象の全言語がそろって初めて作れるもので、`--lang` で選んだ言語だけを作る確認用ページではリンク先が欠けるため、配布時（`package-student-materials.py`）だけで付ける。
7. 新しく訳語を決めた用語を、`i18n/<言語>/glossary.md` に足す。

### 同じ原文を場所によって訳し分けるとき

既定は、`entries` の1つの訳を全ページで使う。**用語の表と表の見出しのように、役割が違うため同じ訳にできない文だけ**、そのページのカタログに `overrides` を足す。用語の表で日本語を残す決まりは変えない。

たとえば `i18n/en/hello-kotlin/index.json` では、既定の `コード — code` を残したまま、表の見出しを `Code` にする。

```json
{
  "source": "docs/hello-kotlin/index.html",
  "language": "en",
  "entries": [
    {"source": "コード", "translation": "コード — code"}
  ],
  "overrides": [
    {"source": "コード", "where": "th", "translation": "Code"}
  ]
}
```

- `source` は取り出した原文をそのまま使う。既存の `entries` または作業ファイルからコピーし、原文は書き換えない。
- `where` は、文を取り出した要素の名前（`th`、`td`、`p` など）。属性なら `img alt`、`nav aria-label` のように、要素名・半角スペース・属性名で書く。作業ファイルの `where` もこの形式である。文字を直接持たない要素はその子から取り出すため、`<th><strong>コード</strong></th>` のような形では `strong` になる。ページのHTMLも見て確かめる。
- 適用先は、**そのカタログのページ内にある、同じ `source` と `where` のすべての文**。同じSTEP内の `<th>コード</th>` と `<td>コード</td>` を分けられる。
- STEP番号・`id`・何番目の要素かは使わない。文や表を別のSTEPへ移動しても、原文と要素の種類が同じなら訳は保たれる。同じページ・同じ役割でも違う訳が必要な場合は、この指定では区別できないため、追加対応が必要な箇所として報告する。翻訳の都合で日本語の本文は変えない。
- `overrides` はそのページだけで使い、ほかのページの訳を補うときには使わない。`sync` と `merge` は有効な場所別の訳を残す。`merge --overwrite` も `entries` だけを入れ替える。場所別の訳を直すときは、該当する `overrides` の `translation` を直接直す。
- `check` は、同じ原文と場所の重複、指定の書式、タグや保護されたコード、現在の原文に対して存在しない `where` を検査する。原文が編集・削除された古い指定は、既定訳と同じく `status` の「使っていない訳」に数え、`sync` で外す。日本語を編集しただけではCIを落とさないため、原文の写し間違いも「使っていない訳」に出る。追加時はその数が増えていないことを確かめる。
- `status` の「訳した文」は、その原文が出るすべての場所に、既定訳か場所別の訳があるときに数える。一部の場所だけ訳してあれば未翻訳のままにする。

### 分担して訳すとき

ページごとにsubagentへ分けてよい。そのとき、次を守る。

- subagentは手順2〜4（`--dry-run` まで）を行う。**カタログへ入れる手順5は、親のセッションが1回で行う。** 同じカタログを同時に書き換えないためである。
- 用語集 `glossary.md` も、subagentには書き換えさせない。足したい用語は報告させ、親がまとめて足す。
- subagentには、このskill、用語集、日本語のページ、受け持つ作業ファイルのパスを渡す。

## 翻訳ルール（全言語に共通）

### そのまま残すもの

検査で強制するもの：

- `<code>`・`<kbd>` の中身。1文字も変えない。日本語が入っていても訳さない（`<code>/Users/ユーザ名/Documents/Kotlin</code>`、`<code>変数の名前</code>`）。
- `<pre>` の中のコードは、そもそも翻訳の対象にならない。訳文へ書き写さない。
- `config/teaching-materials.json` の `terms` にある正式表記（指定AVD名 `jec_25cm_kotlin_Pixel 9a` など）。

検査はしないが、必ず守るもの：

- ファイル名、フォルダ名、パス、パッケージ名（`ex01`、`jp.ac.jec.…`）、クラス名、関数名、id、プロジェクト名（`K01HelloKotlin`、`A01HelloAndroid`）。
- **IDEの画面に出る言葉。** 原文が英語で書いているもの（**New Project**、**Run**、**Logcat**、**Project**、**Build**）は、どの言語でも英語のまま残す。訳が要るときは、うしろにかっこで添える。**原文が日本語で書いているIDEのメニュー名**（K01の **新規 → パッケージ**、**ファイル → 開く…** など）は、下のmacOSの画面に出る言葉と同じく日本語のまま残し、その言語で表示したIDEの名前をかっこで添える（英語なら **新規 → パッケージ** (New → Package)）。添える名前は、実際のIDEの表示で確かめる。
- **macOSの画面に出る言葉。** 原文が日本語で書いているメニュー名やフォルダ名（**移動 → ホーム**、**書類**）は、日本語のまま残し、その言語での名前をかっこで添える。学生のMacの表示言語は、学生ごとに違うためである。

  ```text
  原文：Finderの <strong>移動 → ホーム</strong> を開き、
  英語：In Finder, open <strong>移動 → ホーム</strong> (Go → Home),
  ```

- **学生が打ち込む日本語と、画面に出る日本語。** 完成プロジェクトは日本語のままなので、訳文でも日本語のまま書き、意味をかっこで添える。全角スペースもそのまま残す。

  ```text
  原文：「テキストを変更」ボタンを押します。
  英語：Press the 「テキストを変更」 (Change text) button.
  ```

  意味を添えると、画面に出る別の文字と同じになって紛らわしいときは、言葉で説明する。
- **`<code>` の中の日本語の置き換え語を、本文で指すとき。** `<code>/Users/ユーザ名/…</code>` の `ユーザ名` のように、学生が自分のものに読み替える言葉は、本文でも日本語のまま書き、意味をかっこで添える（replace `ユーザ名` (user name) with your own）。
- **教科書自身のUI（コピーボタン、言語切替、進捗表示）は、この対象ではない。** ボタンのラベルは `config/i18n.json` の `languages[].ui` からその言語の文字が入るので、本文でもその言語のラベルをそのまま書く（`<strong>コピー</strong>` ではなく `<strong>Copy</strong>`／`<strong>复制</strong>`）。値は `ui` の `copy`・`copied`・`copy_shortcut` と完全に一致させる。
- **先生に質問するときの日本語の例文。** 「STEP 3の、valが分かりません」のように、学生が日本語で口にするための例文は、日本語のまま残し、意味をかっこで添える。授業は日本語で進むためである。

同じ言葉が、画面の表示名としても、ふつうの言葉としても出てくることがある。`左側の <strong>ダウンロード</strong> をクリック` はFinderの表示名なので `<strong>ダウンロード</strong> (Downloads)` と残し、`教材のファイルをダウンロードします` は動作なので download と訳す。表示名に添えるかっこ書きは、同じページに何度出てきても、文ごとに毎回添える（文は1つずつ独立して使い回されるため）。

`img alt` も同じルールで訳す。ただし、画面に写っている日本語を全部残すと長くなるので、残すのは、その画像で学生が探すことになる言葉だけにする。

### タグと目印

- `<a1>…</a1>`、`<span2>…</span2>`、`<img3/>` は、属性つきのタグを番号で置き換えた目印である。原文にあるものを、同じ数だけ使う。語順に合わせて、並べる順番は変えてよい。
- `<strong>`、`<em>`、`<br>` などのタグも、原文と同じ数だけ使う。足さない。減らさない。
- 文字としての `<`、`>`、`&` は、`&lt;`、`&gt;`、`&amp;` と書く。原文にある `&amp;` などは、そのまま残す。
- 訳文の前後に空白や改行を入れない。

### 文体

- 読むのは、Kotlinの初学者である。短い文と、やさしい言葉で書く。1つの原文を2つの文に分けてよい。内容は足さない。引かない。
- 操作の手順は、その言語の手順書のふつうの言い方で書く（英語なら命令形：Click **Run**.）。
- **ふりがなは落とす。** 「実行（じっこう）できる」の「（じっこう）」は、日本語の読みを助けるためのもので、訳文には要らない。英語の言葉に添えたカタカナの読み（`Kotlin（コトリン）`、`SDK（エスディーケー）`）も、元のつづりから読めるので落とす。
- **英語の表示に添えた日本語のかっこ書き**（`Name（名前）`、`<strong>Finish</strong>（完了）`）は、訳すと同じ言葉の繰り返しになるなら落とす（英語で `Name (name)` とは書かない）。`Package name（プロジェクトを区別する名前）` のように説明になっているものは、訳して残す。
- **用語の表の用語は、日本語を残して訳を添える。** 「この単元で出てくるKotlinの言葉」のように、言葉とその意味を並べた表の「言葉」の列は、`トップレベル関数 — top-level function` のように、原文のうしろに「 — 」と訳を書く。先生が授業で口にする言葉を、学生が聞き取れるようにするためである。用語が英字そのもの（`Kotlin`）や、`<code>` の中のコード（`val`）のときは、訳を足さない。
- **本文で用語を定義する文も、日本語を添える。** 「この書き方を<strong>拡張関数</strong>といいます」は `This is called an <strong>extension function</strong> (拡張関数).` のように、訳語のうしろへ日本語をかっこで添える。カタカナの用語（メソッド、ログ）は、英語から読めるので添えなくてよい。
- かぎかっこ「」は、その言語の引用符にする。どの引用符を使うかは、用語集の「書き方の決まり」に書く。ただし、上の「画面やアプリに出る日本語」を囲むときは、「」のまま残してよい。
- 全角の記号（：（）、。）は、その言語の記号にする。`→`、`①②③`、`STEP 01` はそのまま残す。ただし右から左に書く言語（`ar`）では、手順の順番を示す `→` を `←` にする（下の「アラビア語（`ar`）の注意」）。漢字を使わない言語では、日本語を残す文（画面の日本語を「」で囲む文など）を除いて、訳文に全角の記号が残っていると `check` と `merge` で落ちる（#89）。翻訳の単位にならない文字（かな・漢字を含まない「（Android 12）」「STOP・RESET」など）の全角の記号は、ページを作るときにASCIIの記号に置き換わるので、訳す側は何もしなくてよい。
- 単元の名前（`K01：HelloKotlin`）は訳さない。漢字を使わない言語では、記号だけをASCIIにして `K01: HelloKotlin` と書く（サイドバーの単元名も、ページを作るときにこの形になる）。「共通：はじめの準備」のような共通資料の名前は訳す。

### 韓国語（`ko`）の注意

- **文体は합니다体にそろえる。** 教科書なので、해요体・반말は使わない。手順の指示も「`Run`을 클릭합니다.」のように書く。
- **助詞は、直前に実際に読む音で選ぶ。** 英語やコードのうしろに助詞が来るときは、その語を韓国語で読んだときの終声で決める（`Kotlin을`、`Run을`、`IntelliJ IDEA를`、`main 함수를`）。迷う語は用語集に「この語のうしろは을／를 のどちらか」を書き足す。
- 「이(가)」「을(를)」のようなかっこ書きの両論併記はしない。どちらかに決めて書く。
- **分かち書き（띄어쓰기）を守る。** 外来語とコードのあいだにも半角スペースを入れる（`Kotlin 파일`、`main 함수`）。
- 引用符は `'…'`（1文字・短い語）と `"…"`（文）を使い分け、用語集の「書き方の決まり」に書いたとおりにそろえる。「」をそのまま残すのは、上のルールにある「画面やアプリに出る日本語」を囲むときだけ。
- **漢字は使わない。** 日本語の漢字語をそのまま漢字で書かず、ハングルで書く（`変数` → `변수`）。ただし、原文の日本語を残す決まりのある箇所（用語の表、画面に出る日本語）では、日本語のまま残す。
- 英語由来の用語は、韓国の開発現場で通じる表記にそろえる（`프로젝트`、`패키지`、`함수`、`변수`、`빌드`、`에뮬레이터`）。決めた表記は必ず `i18n/ko/glossary.md` に書く。

### 台湾華語（`zh-Hant-TW`）の注意

- **香港の繁体字（`zh-Hant-HK`）とは別の言語として訳す。** 同じ繁体字でも、語彙と字形が違う。`zh-Hant-HK` の訳文を写したり、簡体字の訳文を字だけ繁体字に直したりしない。
- **台湾の語彙を使う**（程式碼、軟體、網路、資料夾、預設、非同步）。香港・中国の言い方（代碼、軟件、網絡、文件夾、默認、異步）との対応は、`i18n/zh-Hant-TW/glossary.md` の「書き方の決まり」にある。
- **字形は台湾の字形にする**（這裡。香港の 這裏 は使わない）。
- 引用符は「」と『』。日本語のかぎかっこと同じ形なので、画面やアプリに出る日本語を「」のまま残した箇所も、そのまま読める。
- 日本語の画面の言葉に訳を添えるとき、日本語と同じ字になる言葉（**設定** など）には、かっこ書きを添えない。

### スペイン語（`es`）の注意

- **中南米のスペイン語で書く**（2026-09-24 オーナー決定）。スペインの言い方（ordenador、fichero、pulsar、vosotros）は使わない。対応は `i18n/es/glossary.md` の「書き方の決まり」にある。
- **呼びかけは tú にそろえる。** 手順は tú の命令形（Haz clic en **Run**.）。usted と vos は使わない。
- 疑問符と感嘆符は ¿…? ¡…! の対で書く。見出しは文頭だけ大文字にする。
- 学生や先生を指すときは、男女で形が変わらない語（estudiante、docente）を選ぶ。

### アラビア語（`ar`）の注意

- **右から左に書く。** ページは `<html dir="rtl">` で出る。レイアウトの左右の入れ替えと、`<code>`・`<kbd>`・`<pre>` を左から右で出すことは、生成スクリプトとCSSが受け持つ。訳文で気を付けることは、次のとおり。例と、ブラウザで確かめた見え方は `i18n/ar/glossary.md` の「書き方の決まり」にある。訳文は現代標準アラビア語（فصحى）で書く。
- **日本語や英語のまま残す言葉が記号（。、…！？）で終わるときは、記号のすぐうしろに `&lrm;` を書く**（`「保存した記録を読み込みました。&lrm;」`）。書かないと、記号が言葉の反対側へ回り込む。訳文にはタグを足せないので、見えない文字で向きを示す。`<code>`・`<kbd>`・`<strong>` の中は要らない。2026-09-25のオーナー決定で、jec-25cm-hybrid-app でも同じ決まりにしている（#96）。**文字そのもの（U+200E）ではなく、`&lrm;` という書き方で書く**（カタログの上で目に見え、レビューで見落とさず、コピーにも紛れ込まない）。向きを変える見えない文字をそのまま入れた訳は、`check` と `merge` で落ちる。PRやissueのコメントに `&lrm;` を含む訳文を書くときは、コードで囲む（下の「逆翻訳による照合」の決まりの3）。
- **アラビア語で書く操作や処理の並びは、矢印を ← にする**（`غيّر شيئا واحدا ← توقّع ← شغّل`）。右から左に読むので、→ のままだと逆の順番に見える。画面の名前をつないだ経路（**File → New → Project…**）は、原文の言葉と → をそのまま残し、訳はうしろのかっこに添える。かっこの中のアラビア語の経路は ← でつなぐ。
- **`<strong>` の中は、アラビア語から書き始める。** `<strong>` は中の最初の文字で向きが決まるので、英字や日本語で始めてアラビア語を続けると、読む順が逆になる。英字・日本語だけを入れるのはよい。
- 数字は 0〜9 で書く。手順は命令形の男性形を総称として使う。引用符は «…»、読点は ،、疑問符は ؟（用語集の「書き方の決まり」）。
- `build` で作った確認用ページでは、ほかの言語で見る点に加えて、本文が右寄せで右から左に読めること、コードが左から右のままであること、`→` と `←` が読む向きを指していること、日本語や英語のまま残した言葉の末尾の記号が言葉の側に付いていることを確かめる。

### モンゴル語（`mn`）の注意

- **モンゴル国のキリル文字（横書き）で書く**（2026-09-25 オーナー確認）。伝統的モンゴル文字（縦書き、`mn-Mong`）は使わない。
- **モンゴル語だけの文字 Ө ө Ү ү を、似た文字で代用しない。** ロシア語の О о・У у、ラテン文字の O o・Y y、Ѳ ѳ、Ұ ұ は別の文字である。У と Ү はモンゴル語では別の字で、取り違えると別の語になる。
- **キリル文字の語に、ラテン文字を混ぜない。** キリル文字の а とラテン文字の a のように、見た目では区別できない文字がある（用語集の下書きで、実際に混ざった）。訳文を足したら、次の検査で何も出ないことを確かめる。

  ```sh
  python3 - <<'PY'
  import glob, re
  for path in sorted(glob.glob("i18n/mn/*.json")) + ["i18n/mn/glossary.md"]:
      for word in re.findall(r"[\w\u0400-\u04FF]+", open(path, encoding="utf-8").read()):
          if any("\u0400" <= c <= "\u04FF" for c in word) and any(c.isascii() and c.isalpha() for c in word):
              print(path, word)
  PY
  ```

- **ロシア語由来の語は、モンゴル語のつづりで書く**（функц、програм、консол。функция、программа、консоль とは書かない）。
- **呼びかけは丁寧な та にそろえる。** 手順は「語幹＋-на уу／-нэ үү／-но уу／-нө үү」の形（дарна уу、нээнэ үү、сонгоно уу）。語幹だけの命令形（дар）と、話し言葉の -аарай／-ээрэй は使わない。
- **英字の語・コード・日本語のまま残す画面名に、格語尾を直接付けない。** うしろにモンゴル語の名詞（товч、файл、цэс、хэсэг）を置き、格語尾はその名詞に付ける（**Run** товчийг дарна уу、`main.kt` файлыг нээнэ үү）。
- 文の途中の言語名は小文字で書く（монгол хэл、япон хэл）。引用符は «…»。どちらも `i18n/mn/glossary.md` の「書き方の決まり」にある。

### 一貫性

- 用語集 `i18n/<言語>/glossary.md` の訳語と「書き方の決まり」（引用符、見出しの大文字と小文字、チェック欄の文の形など）を使う。
- **ほかのページの見出しを引用している文**（リンクの文字「共通：はじめの準備」など）は、そのページのカタログにある見出しの訳と同じにする。まだ訳されていなければ、訳した言い方を報告し、あとで照合する。
- 同じ原文には、既定の同じ訳が入る（`merge` が、その原文を持つ全ページへ入れる）。役割が違う場所だけ、`overrides` で例外を指定する。

## 別のモデルによる照合

訳したのとは**別のモデルのsubagent**が、カタログの `source` と `translation` を並べて読み、意味の違い・訳し落とし・足しすぎ・操作順の入れ替わり・用語集との不一致を挙げる。

### 照合する時機（2026-09-25 オーナー決定、#100）

照合は、訳すたびには行わない。**学生の手に渡る前に1回**行う。

| 場面 | 照合 |
| --- | --- |
| `distribute: false` の言語の翻訳PR（初回の翻訳、ページごとの翻訳） | **しない。** 手順の `check` と、`build` で作った確認用ページの目視まで |
| その言語の `distribute` を `true` に上げる配布準備 | **その言語の全文を1回照合する。** 前に照合したページも含める（どの文を照合済みか、記録を持たずに済むようにするため） |
| `distribute: true` の言語の差分翻訳（配布準備の翻訳PR） | **訳した文が50文以上なら照合する。** 50文未満なら、翻訳の担当が訳を見直して `check` を通すだけにする |

- 基準は、きれいな訳文ではなく、**学生が操作を間違えないこと**に置く。翻訳ページには「AIが翻訳した。日本語版が正」という注記と、日本語版を同じ位置で開くリンクが付き、授業も日本語で進む。コード・キー・正式表記・タグの数は `check` が機械的に検査している。
- 50文は、新しいSTEPやページを訳す量と、日本語の手直しを訳し直す量を分ける目安である。数は `merge --dry-run` の「検査に通った訳」を足して数える。
- **日本語を直したPRで、配布していない言語を訳し直さない。** 未翻訳に戻った文は日本語で表示されるままにし、その言語の翻訳PRか配布準備で訳す。同じページを訳している翻訳PRが開いているなら、そのPRで訳してよい（照合は要らない）。

### 照合のしかた

照合は、翻訳と同じセッション・同じPRの中で行ってよい（オーナーの判断、2026-09-24）。同じセッションだと、訳した側（親のセッション）が照合の前とあとの両方を握るので、次の4つを守る。別のセッションで照合するときも同じ。

1. **照合の担当は、訳した担当とは別のモデルにする。** 同じモデルは、同じところで同じように間違えやすい。
2. **照合の担当に、資料を自分で読ませる。** このskill・用語集・日本語のページのパスと、照合する対訳を渡す（ミャンマー語では、日本語のページは訳し戻しのあとで渡す。下を参照）。親が決まりを要約して渡すだけにしない。親の読み違いが、照合にもそのまま入るためである。確かめてほしい点を足してもよいが、照合はそれだけに限らない。
3. **照合の担当が挙げた指摘は、全件を翻訳PRのコメントに残す**（PRがなければ、照合のissueのコメントに残す）。「対応不要」も含め、親が要約せずに、照合の担当が書いたとおりに残す。オーナーが、指摘が落ちていないかを確かめられるようにするためである。**ただし、ミャンマー語（`my`）の綴りは、PRやissueの本文・コメントに写さない。** GitHubはミャンマー語の結合文字を落とすことがあり、別の語に変わったまま保存される（別の教材リポジトリで再現した。コードブロックの中でも起きる）。ミャンマー語の指摘は、IDと原文（日本語）と、訳し戻した日本語で指し、綴りはカタログと `i18n/my/glossary.md` に置く。コミットメッセージとファイルには、そのまま書いてよい。**指摘の中の原文・訳文は、コードで囲んで書く**（1行ならインラインのコード、複数行ならコードブロック。ミャンマー語の訳文は、コードで囲んでも写さない）。GitHubは、コードで囲んでいない `&lrm;` を見えない文字に変え、カタログのタグの目印（`<a1>`、`<strong>` など）をタグとして消すので、画面では指摘の肝心の部分が読めなくなる（2026-09-25、jec-25cm-hybrid-app で、GitHubのMarkdownの表示で確かめた）。PRやissueの本文に書くときも同じ。
4. **照合のあとで意味が変わる修正をした文だけ、照合し直す。** 意味の取り違え・訳し落とし・足しすぎ・操作の順を直した文は、直した担当とは別のモデルのsubagentがもう一度照合し、結果をPRのコメントに残す。記号・大文字と小文字・言い回しのそろえ・用語集の訳語に合わせただけの修正は、照合し直さない（照合し直しが、照合の担当自身の直させた訳を戻すことが多かったため。#100）。

- **ミャンマー語（`my`）だけは、訳文を日本語へ訳し戻して**原文と比べる。訳し戻しは、原文を見ずに行う（原文を見ながらだと、訳文が足りなくても原文に引きずられて補ってしまう）。照合の担当に原文と日本語のページを渡すのは、訳し戻しを終えてからにする。ほかの言語は、対訳を並べて読む。
- 照合したページと、見つけた違い、直した内容をPR本文に残す。担当（どのsubagent・どのモデルが訳し、どれが照合したか）も書く。**記録はPR本文とPRのコメントだけにし、用語集には「照合の記録」を書かない。** 照合で決めた訳語や書き方は、用語集の訳語の表か「書き方の決まり」に書く。2026-09-25より前に書いた「照合の記録」の節は、過去の判断の理由として残し、書き足さない。
- 見つけた誤訳は、カタログの `translation` を直接直す。**`source` は手で書き換えない**（原文と合わなくなり、その訳が使われなくなる）。
- 照合のために訳し直す必要はない。

## `distribute` を `true` にする条件

`config/i18n.json` の `languages[].distribute` が `true` の言語だけが、学生用ZIPに入る。**言語を足したときの初期値は `false`**（翻訳が1文もないため）。いまは英語（2026-09-25、#56）と中国語（簡体）（2026-09-25、#126）が `true`。初版はこの2言語と日本語で配布し、ほかの7言語は後期の授業が始まってから段階的に加える（2026-09-25 オーナー決定。AGENTS.md §12）。次のすべてを満たした言語から、1言語ずつ `true` に上げる。

1. `python3 scripts/localize-student-materials.py status --lang <言語> --require-complete` が通る（その言語の未翻訳が0）。
2. `python3 scripts/localize-student-materials.py check` が通る。
3. 上の「別のモデルによる照合」で、その言語の全文を1回照合し、対応必要の指摘を反映してある。
4. `build --lang <言語>` で作った確認用ページを開き、レイアウト・リンク・コードのコピー・共通資料からの戻り先を目で確かめてある。
5. `i18n/<言語>/glossary.md` に、その言語で決めた訳語と書き方の決まりが書いてある。

`true` に上げるのは、上げる理由と確認結果を書いたPRで行う。**翻訳が途中の言語を `true` にしない。** `release-student-materials.py` の公開ゲートが、`distribute: true` の言語に未翻訳があると公開を止める。未翻訳を許可する `ALLOW_UNTRANSLATED` は緊急修正のための入力で、通常の配布準備では使わない。

## 配布前の差分翻訳

日常のPRの翻訳は不要。配布準備を依頼されたら、次の順で進める。

1. 最新のmainと配布準備のissueを確認する。`config/i18n.json` の `distribute: true` の言語を対象に、`status` で言語・ページごとの未翻訳を記録する。`distribute: false` の言語は訳さない（日本語を直して未翻訳に戻った文も、日本語で表示されるままにする）。
2. 対象言語ごとに `sync --lang <言語>` を実行し、上の手順で未翻訳の文だけを訳す。新しい単元と、既存ページのサイドバーに増えた文も対象になる。前回の訳が残っている文を好みで書き換えない。
3. `area:i18n`、`size:*`、`enhancement` を付けた翻訳PRを開く。まだ学生用ZIPに入らない言語だけのPRには `skip-release-notes` も付ける。PR本文に配布対象の版、言語、変更前後の未翻訳件数を書く。**この配布準備の翻訳PR（`distribute: true` の言語の差分翻訳）を開いてから公開が終わるまでは、`docs/` を触るPRをマージしない**（公開ゲートが配布対象の言語の未翻訳0件を求めるので、訳しているあいだに日本語が変わると、公開の直前に未翻訳が出る）。配布していない言語の翻訳PRでは止めない（上の「手順」の冒頭）。
4. 訳した文が50文以上なら、上の「別のモデルによる照合」を行い、指摘の全件をPRのコメントに、担当・範囲・指摘への対応をPR本文に残す。50文未満なら、翻訳の担当が訳を見直し、その旨と文の数をPR本文に書く。
5. 最新のmainを取り込み、`check` と `status --require-complete` を実行する。未翻訳が増えたときは `sync` から差分だけをやり直す。対訳カタログの検査、AGENTS.md の検証コマンド、配布ZIPの入口 `index.html` から各言語の表示・リンクを確認する。
6. 翻訳PRをマージし、READMEの「手動公開」へ進む。公開自体は、オーナーから任されている場合に限る。Actionsは翻訳せず、対訳カタログから生成・検査するだけ。

## 翻訳PRのレビュー

- `check` が通っていること。未翻訳の数は `status` で見る。
- 分担して訳したときは、担当どうしで言い回しが割れていないかを見る（引用符、同じ見出しの訳、チェック欄の文の形、韓国語なら文体と助詞）。
- 照合が要る場面（上の「照合する時機」）なら、訳した担当とは別のモデルで照合され、担当と照合範囲がPR本文に、指摘の全件がPRのコメントに残っていること。照合のあとで意味が変わる修正をした文は、照合し直してあること。
- 照合が要らない場面なら、照合がないことを指摘しない。
- 見つけた誤訳は、カタログの `translation` を直接直す。`source` は手で書き換えない。

## やってはいけないこと

- `docs/` の日本語を、翻訳の都合で書き換える。
- カタログの `source` を手で書き換える。並べ替えも `sync` と `merge` に任せる。
- `<pre>` や `<code>` の中身を訳す。
- 各言語のHTML（`dist/i18n-preview/` の中身、`docs/<言語>/`）をコミットする。
- 変わっていない文の訳を、好みで書き直す。差分が増え、レビューできなくなる。
- 翻訳が途中の言語の `distribute` を `true` にする。
